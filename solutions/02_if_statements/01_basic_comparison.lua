-- first, we compare two values, which evaluate to true or false
-- next, the result of this operation is assigned to a variable
myBoolean = 3 == 3

-- with myBoolean now assigned to true, we can print it to the output
print(myBoolean)