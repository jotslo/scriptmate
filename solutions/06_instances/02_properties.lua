-- we grab the baseplate in the workspace
-- if it doesn't exist, create it manually

local baseplate = game.Workspace.Baseplate

-- we then change its transparency property to 0.5
baseplate.Transparency = 0.5