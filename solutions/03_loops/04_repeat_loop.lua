-- variable that stores the loop count
local loopCount = 0

-- repeat loop
repeat
    -- output loopCount
    print(loopCount)

    -- increment loopCount by 1
    loopCount = loopCount + 1
until loopCount > 50