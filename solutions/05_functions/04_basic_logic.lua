-- function named isLarge that takes one number as argument
-- if >= 100, return true
-- otherwise, return false

function isLarge(number)
    -- number >= 100 returns true or false itself
    -- so we can just return the result of that expression
    return number >= 100
end